[package]
name = "loam-physics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Loam rigid-body physics: Space-generic integration, narrowphase dispatch, impulse solver."
keywords = ["physics", "rigid-body", "collision", "non-euclidean", "gamedev"]
categories = ["game-development", "simulation"]

[dependencies]
glam.workspace = true
tracing.workspace = true
loam-math  = { path = "../loam-math",  version = "0.1.0" }
loam-shape = { path = "../loam-shape", version = "0.1.0" }

[[bench]]
name = "broadphase"
harness = false
