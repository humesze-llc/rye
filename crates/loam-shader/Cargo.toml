[package]
name = "loam-shader"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Loam shader system: WGSL loading, Space intrinsic injection, hot reload."
keywords = ["wgsl", "shader", "wgpu", "hot-reload", "graphics"]
categories = ["graphics", "rendering", "game-development"]

[dependencies]
loam-math  = { path = "../loam-math" }
loam-asset = { path = "../loam-asset" }

anyhow.workspace = true
naga.workspace = true
thiserror.workspace = true
tracing.workspace = true
wgpu.workspace = true

[dev-dependencies]
# Tier 3 depending on tier 2 for tests only: the scene/GPU parity probe needs a
# real `Scene` emit, and the inverse (wgpu in loam-scene) is the wrong direction.
loam-scene = { path = "../loam-scene" }

bytemuck.workspace = true
glam.workspace = true
pollster.workspace = true
tempfile = "3"
# The owner-scoping tests need a real `wgpu::Device` to construct a `ShaderDb`,
# but nothing they assert reaches the GPU; the noop backend supplies a Device
# without a GPU, so they run in CI rather than behind `#[ignore]`.
wgpu = { workspace = true, features = ["noop"] }
